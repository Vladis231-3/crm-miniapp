import React, { type ReactNode } from "react";
import { Bell, Moon, Sun, type LucideIcon } from "lucide-react";
export type NavItem<T extends string>={id:T;label:string;icon:LucideIcon;badge?:number;active?:boolean;onSelect?:()=>void};
export function RoleNavigation<T extends string>({items,active,onSelect}:{items:NavItem<T>[];active:T;onSelect:(id:T)=>void}){return <nav className="role-nav" aria-label="Навигация"><div className="role-nav__scroll">{items.map(x=>{const yes=x.active??x.id===active;return <button key={x.id} className={`role-nav__item ${yes?"is-active":""}`} onClick={()=>x.onSelect?x.onSelect():onSelect(x.id)}><span className="role-nav__icon"><x.icon size={19} strokeWidth={1.75}/>{!!x.badge&&<span className="role-nav__badge">{x.badge}</span>}</span><span className="role-nav__label">{x.label}</span></button>})}</div></nav>}
export function WorkspaceHeader({eyebrow,title,subtitle,leading,actions,isDark,onToggleTheme,unreadCount,onNotifications}:{eyebrow?:string;title:string;subtitle?:string;leading?:ReactNode;actions?:ReactNode;isDark?:boolean;onToggleTheme?:()=>void;unreadCount?:number;onNotifications?:()=>void}){return <header className="work-header"><div className="flex min-w-0 items-center gap-3">{leading}<div className="min-w-0">{eyebrow&&<div className="section-kicker">{eyebrow}</div>}<div className="truncate text-base font-semibold">{title}</div>{subtitle&&<div className="truncate text-xs text-muted-foreground">{subtitle}</div>}</div></div><div className="flex items-center gap-2">{actions}{onNotifications&&<button onClick={onNotifications} className="header-action"><Bell size={18} strokeWidth={1.75}/>{!!unreadCount&&<span className="role-nav__badge">{unreadCount}</span>}</button>}{onToggleTheme&&<button onClick={onToggleTheme} className="header-action">{isDark?<Sun size={18} strokeWidth={1.75}/>:<Moon size={18} strokeWidth={1.75}/>}</button>}</div></header>}
export function MetricSurface({label,value,icon:Icon,tone="default"}:{label:string;value:ReactNode;icon?:LucideIcon;tone?:"default"|"accent"|"success"|"warning"}){return <div className={`metric-surface metric-surface--${tone}`}><div className="flex justify-between text-xs text-muted-foreground"><span>{label}</span>{Icon&&<Icon size={15} strokeWidth={1.75}/>}</div><div className="mt-2 text-xl font-semibold">{value}</div></div>}
export function StatusPill({children,tone="neutral"}:{children:ReactNode;tone?:"neutral"|"success"|"warning"|"danger"|"info"}){return <span className={`status-pill status-pill--${tone}`}>{children}</span>}
